apiVersion: v1
kind: Service
metadata:
  name: payment-api-svc
  namespace: fiap-fcg
spec:
  selector:
    app: payment-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
